// Default production values. On the deploy host, set the API_URL environment variable
// instead of editing this file — set-env.js overwrites apiUrl below at build time.
// See DEPLOY.md.
export const environment = {
  production: true,
  apiUrl: 'https://get-it-together-flzg.onrender.com/api',
};
